name: generic_flowershop_persona
user:
  modality: text
judge:
  modality: text
  eval:
    service: ollama
    model: gemma2:9b
turns:
  - expect:
      - event: response
        text_contains: "Thank you for calling GreenForce Garden"
        within_ms: 60000
  - user: I need flowers for an anniversary.
    expect:
      - event: response
        eval: "the reply stays in the GreenForce Garden flower shop persona and helps with anniversary flowers"
        within_ms: 60000
